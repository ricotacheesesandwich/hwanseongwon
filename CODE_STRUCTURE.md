# 코드 구조 선택 기준

- CSS 파일명 앞의 숫자는 적용 순서를 뜻합니다. 기존 화면의 우선순위와 패치 규칙을 깨지 않기 위해 순서를 고정했습니다.
- `vanilla`는 현재 기능을 1:1로 보존한 운영용 시제품입니다.
- `react`는 화면 구조를 컴포넌트로 나눈 개발용 버전입니다.
- React에서 `gameEngine.js`를 바로 잘게 자르지 않은 이유는 현재 로직에 동일 이름의 보정 함수와 누적 마이그레이션이 많아, 무리하게 분해하면 감염 시계·파일 다운로드·지도 권한이 깨질 위험이 크기 때문입니다.
- 다음 리팩터링 순서는 `감염 시계 → 지도 권한 → 자료 보관함 → 관리자 운영페이지 → 팀 관리`가 적합합니다.
